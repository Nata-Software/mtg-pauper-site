# Database connection string used by Prisma.
# Local dev (SQLite):   file:./dev.db
# Production (Postgres): postgres://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL="file:./dev.db"

# Shared password required to upload/replace data.
# Leave empty for local dev (uploads open); set a value in production.
UPLOAD_PASSWORD=""
